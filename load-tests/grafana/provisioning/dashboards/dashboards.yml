apiVersion: 1

providers:
  - name: 'Load Testing'
    orgId: 1
    folder: 'Load Tests'
    folderUid: 'load-tests'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
